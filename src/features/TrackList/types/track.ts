type Track = {
  trackNumber: number;
  mode: string;
  port: string;
  channelNumber: number;
  stepOffset: number;
  keyOffset: number;
  rhythmTrack: boolean;
  trackMemo: string;
  step: number;
  totalStep: number;
};

export type { Track };
